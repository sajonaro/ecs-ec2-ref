[{
    "name" :  "${task_name}",
    "image" : "${ecr_repo_url}",
    "essential" : true,
    "memory" : 128,
    "cpu" : 1,
    "portMappings" : [
        {
            "containerPort" : 8080, 
            "hostPort" : 8080
     }],
    "mountPoints" :  [ 
        { 
            "containerPath" : "${container_path}", 
            "sourceVolume" : "${storage_name}"
        }] 
  }]

